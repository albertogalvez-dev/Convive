[www]
; The immutable image intentionally contains no .env file. Preserve the
; reviewed Docker Compose environment for worker requests. The front
; controller hydrates Symfony Runtime's superglobals from that environment
; before it boots, because PHP-FPM does not mirror them automatically.
clear_env = no
