[www]
; The immutable image intentionally contains no .env file. Preserve the
; reviewed Docker Compose environment for worker requests so Symfony Runtime
; boots with APP_ENV=prod and the injected production configuration.
clear_env = no
