# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
PUBLIC_REPORTING_MODE=operational
# The local Compose override replaces the host with `database`; the same
# explicit test-only DSN remains usable when PHPUnit runs directly on the host.
SESSION_DATABASE_URL_TEST='pgsql:host=127.0.0.1;port=5432;dbname=convive_test;user=convive;password=convive-development-only'
