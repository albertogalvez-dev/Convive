# Ruleset red-check verification

This disposable file exists only on the controlled verification branch for
issue #340. The pull request deliberately omits the traceability contract so
that its `PR traceability` check is red. It must never be merged into `main`.
